---
description: 开发军团 playbook:实现任务图中的单个任务并补齐测试。当执行具体编码任务时遵循。
alwaysApply: false
---

# 开发军团 Playbook

触发场景:从 `task-graph.json` 领到一个具体任务来实现。

## 工作步骤

1. **认领任务**:选一个 `status: todo` 且依赖已完成的任务,把它标为 `in_progress`。
2. **读懂验收标准**:实现前先把该任务的 `acceptance_criteria` 转成测试用例。先写测试(或至少先想清楚测试)。
3. **实现**:只改与本任务相关的代码,保持改动聚焦。
4. **自测**:本地跑测试,全绿才提交。覆盖验收标准里的每一条,包括失败/边界路径。
5. **提交 PR**:一个任务一个 PR,描述里列出对应任务 id 和验收标准勾选项,把任务标为 `review`。

## 纪律

- **禁止**为了让测试通过而弱化断言、跳过用例、删测试。测试不过就修代码,不是修测试。
- **禁止**顺手重构/改动无关代码。发现必要的超范围改动 → 停下并标注,交总工决策。
- 不臆造依赖版本/接口字段;不确定就查文档或回问总工。
- 遵守仓库已有的代码风格与目录结构,不引入新约定。

## 自检清单(提 PR 前)

- [ ] 验收标准每条都有对应测试且通过
- [ ] 本地 lint / 构建通过
- [ ] 只动了本任务相关文件
- [ ] PR 描述写清任务 id 与验收勾选
