# Escalation for T-006 QA

## Blockers (规格不清)
- interface_contract 明确指定输出文件为 `tests/galaxy.spec.js` + `playwright.config.js`，但铁律 2 强制要求“只写到 tests/acceptance/，文件名含任务 id”。位置与命名存在直接冲突，无法同时满足。
- 截图对比验收标准“匹配度 > 95%”未说明：
  - 基准图（baseline）存放位置、命名、生成/更新机制
  - 使用 toMatchSnapshot 还是 toHaveScreenshot + pixelmatch，阈值如何配置（0.05?）
  - 首次运行无 baseline 时的处理策略
- playwright.config.js 的具体内容、browser 项目、webServer 启动命令、snapshotDir、testDir 等关键配置未在 interface_contract 或 acceptance_criteria 中定义。
- “npm test 或 npx playwright test” 的确切 runner、package.json scripts、@playwright/test 版本、依赖安装方式均未提供。
- acceptance_criteria 中“所有测试用例通过”属于元条件，无法独立验证；缺少对 FPS 检测、移动端 touch、具体断言的细节。
- 测试需“自带 fixture/mock”与“可独立运行”，但未说明如何 mock 静态文件服务、CDN 或 canvas/WebGL 环境。

## 倾向假设 (Assumptions I'd make if forced)
- 忽略 interface_contract 的文件路径，严格按铁律放入 tests/acceptance/T-006-galaxy.spec.js
- 采用标准 Playwright + @playwright/test，webServer 启动 python3 -m http.server
- 截图使用 toHaveScreenshot({ threshold: 0.05 })，baseline 放 tests/acceptance/__snapshots__
- 每个 AC 至少一个 test case，覆盖正常/失败路径，使用独立 browser context

## 需谁确认 (Who to confirm)
- 产品经理 / 总工：确认 tests/acceptance/ 放置策略是否覆盖 playwright.config.js；明确 baseline 管理方式、截图阈值、测试 runner 命令及依赖版本；是否允许在 escalation 后继续或调整 interface_contract。

Status change: T-006 -> blocked (per rule 6)