# Escalation for T-006

## 卡点 (Block Points)
- interface_contract 指定任务产出文件为 `tests/galaxy.spec.js` 和 `playwright.config.js`，但 QA 铁律明确要求“测试只写到受保护目录 tests/acceptance/，文件名含任务 id”。
- 位置和文件名存在冲突，无法同时满足两者。
- T-006 的 acceptance_criteria 是关于“运行测试后所有用例通过”等元描述，interface_contract 中的 coverage 列表与 AC 对应，但未明确验收测试本身的测试框架、fixtures 如何组织（独立运行）。
- 未提供 playwright.config.js 的具体配置细节（如 webServer 启动命令、snapshot 路径、browser projects），无法在不臆测的情况下编写完整可运行的测试。

## 倾向假设 (Assumed Tendency)
- 假设应优先遵守 QA 铁律，将验收测试文件命名为 tests/acceptance/T-006-*.spec.js，内容覆盖 5 条 AC（含正常/失败路径）。
- 但这将导致与 interface_contract 中声明的文件不一致，可能影响后续任务或 CI。

## 需谁确认 (Who to Confirm)
- 总工 / 架构师：确认 T-006 的 interface_contract 文件位置是否需要更新，或 QA 写测试的位置规则是否适用于 test 类型的任务。
- 产品经理：确认 T-006 的验收标准是否需要细化（例如明确 playwright.config.js 的要求）。

Status 建议: blocked
