# T-005 QA Escalation

## 卡点 (Block Point)
acceptance_criteria 第 3 条要求 "Given 任何设备，When 运行 10 秒，Then 平均 FPS >= 30"，但 interface_contract 仅暴露 getOptimalParticleCount() 和 isMobile()，未提供任何 FPS 查询接口或性能指标暴露方式。无法从黑盒测试中可靠获取/断言平均 FPS。

## 倾向假设 (Likely Assumption)
- FPS 可通过 T-003 的右下角 FPS 计数器 UI 读取，或存在全局 fps 变量/函数。
- 粒子数量验证可通过调用 getOptimalParticleCount() 后检查实际渲染的 Points 粒子数。

## 需谁确认 (Who to Confirm)
- 总工/架构师：确认 FPS 和粒子数量在黑盒验收测试中的可观测接口（补充 interface_contract 或提供测试 helper）。

## 建议
将 T-005 status 改为 blocked，待接口明确后再编写测试。