# Escalation for T-001 QA

## Blockers (规格不清)
- 验收测试的执行环境未在 acceptance_criteria 或 interface_contract 中指定：
  - 页面访问 URL/baseURL 如何配置？（假设 http://localhost:8080 但未明确）
  - Playwright 测试需自带 server fixture 还是依赖外部 playwright.config.js / webServer？
  - 测试 runner 命令、依赖 (@playwright/test 版本) 未定义
  - index.html 的 MIME 类型或静态服务细节未说明
- 黑色背景验证方式：是检查 computed style、canvas fillStyle 还是视觉截图？未明确失败路径的断言标准。
- CDN 加载验证：是否需模拟网络失败场景（离线/404）？未指定 mock 策略。
- 控制台错误过滤： "Three.js 相关错误" 的精确匹配规则（正则？日志级别？）未定义。

## 倾向假设 (Assumptions I'd make if forced)
- 使用 Playwright 的 webServer 启动 python3 -m http.server 8080
- baseURL = 'http://localhost:8080'
- 每个 test 用独立 browser context + page
- 正常路径：成功加载 + 无 error 日志 + canvas 尺寸全屏 + 背景色 #000000
- 失败路径：CDN 404 时页面报错、console 出现错误

## 需谁确认 (Who to confirm)
- 产品经理 / 总工：明确测试 harness 配置、URL、断言粒度、是否允许在 tests/acceptance/ 放置 playwright.config 或 helper fixtures。

Status change: T-001 -> blocked (per rule 6)